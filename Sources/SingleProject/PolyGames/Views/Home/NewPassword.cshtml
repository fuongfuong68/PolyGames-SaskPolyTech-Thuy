@model PolyGames.Models.NewPasswordModel
@{
    ViewBag.Title = "New password";
}

<h2 class="text-center">New Password</h2>
<div class="row">
    <div class="col-sm-12 col-md-3 col-lg-4"></div>
    <div class="col-sm-12 col-md-6 col-lg-4">
        @RenderPage("~/Views/Shared/_Notif.cshtml")
        <div class="card bg-dark">
            <div class="card-body">
                @using (Html.BeginForm("NewPassword", "Home", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(m => m.UserId, htmlAttributes: new { @class = "form-label" })
                    <fieldset>
                        <div class="mb-3">
                            @Html.LabelFor(m => m.Password, htmlAttributes: new { @class = "form-label" })
                            @Html.EditorFor(m => m.Password, new { htmlAttributes = new { @class = "form-control" } })
                            @Html.ValidationMessageFor(m => m.Password, null, htmlAttributes: new { @class = "text-danger" })
                        </div>
                        <button type="submit" class="btn btn-primary mt-2">Submit</button>
                    </fieldset>
                }
            </div>
        </div>
    </div>
    <div class="col-sm-12 col-md-3 col-lg-4"></div>
</div>

@section Scripts{
    @Scripts.Render("~/bundles/jqueryval")
}
