@if (ViewBag.Message != null)
{
    <div class="alert alert-secondary" role="alert">
        @ViewBag.Message
    </div>
}

@if (TempData["Message"] != null)
{
    <div class="alert alert-secondary" role="alert">
        @TempData["Message"]
    </div>
}
